apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: k8up
    app.kubernetes.io/name: k8up
  name: k8up-metrics
  namespace: k8up
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/instance: k8up
    app.kubernetes.io/name: k8up
  type: ClusterIP
