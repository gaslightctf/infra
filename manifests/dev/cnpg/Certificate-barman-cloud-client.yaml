apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: barman-cloud-client
  namespace: cnpg
spec:
  commonName: barman-cloud-client
  duration: 2160h
  isCA: false
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: barman-plugin-barman-cloud-selfsigned-issuer
  privateKey:
    rotationPolicy: Always
  renewBefore: 360h
  secretName: barman-cloud-client-tls
  usages:
    - client auth
