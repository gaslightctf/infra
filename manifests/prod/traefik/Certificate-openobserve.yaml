apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openobserve
  namespace: traefik
spec:
  dnsNames:
    - openobserve.gaslightctf.cooking
  issuerRef:
    group: cert-manager.k8s.cloudflare.com
    kind: OriginIssuer
    name: cf-issuer
  secretName: openobserve-tls
