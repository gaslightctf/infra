apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: berg-api
  namespace: traefik
spec:
  dnsNames:
    - api.gaslightctf.cooking
  issuerRef:
    group: cert-manager.k8s.cloudflare.com
    kind: OriginIssuer
    name: cf-issuer
  secretName: berg-api-tls
