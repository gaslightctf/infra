{ lib, ... }:
{
  instances.rayquaza = {
    enable = true;
    tags = [ "server" ];
  };
  instances.kyogre = {
    enable = true;
    tags = [ "server" ];
  };
  instances.groudon = {
    enable = true;
    tags = [ "server" ];
  };
}
